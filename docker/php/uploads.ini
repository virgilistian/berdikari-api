; Explicit upload ceiling — bounds any single request (product photos,
; business logo, tax signature/stamp) so a large camera-original upload
; can't exhaust memory or disk on the free-tier host. Callers are still
; expected to compress client-side; this is the server-side backstop.
upload_max_filesize = 10M
post_max_size = 12M
